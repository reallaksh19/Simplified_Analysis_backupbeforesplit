import assert from 'node:assert/strict';
import { execFileSync } from 'node:child_process';
import { readFileSync, readdirSync, statSync } from 'node:fs';
import { basename, join } from 'node:path';
const BASE_SHA='e12adc701ea9b189a3d703dc218c227a854aef29';const root=new URL('../',import.meta.url).pathname;const core=join(root,'src/core/element-fea');
const allowed=[/^src\/core\/element-fea\//,/^scripts\/lfea-006-[^/]+\.mjs$/,/^scripts\/qa-check\.mjs$/,/^docs\/element-fea\/LFEA-006_IMPLEMENTATION\.md$/,/^\.github\/workflows\/lfea-006-certification\.yml$/];
ensureBase();const changed=gitLines(['diff','--name-only',BASE_SHA,'HEAD']);const errors=[];
changed.forEach((file)=>{if(!allowed.some((rule)=>rule.test(file)))errors.push(`Disallowed LFEA-006 changed path: ${file}`);});
assert.ok(!changed.includes('.github/workflows/lfea-006-bootstrap.yml'),'Temporary LFEA-006 bootstrap workflow is prohibited from the final tree.');
assert.ok(!changed.some((file)=>/lafea|pcf|settings-authority|application-shell|workspace|view-state|registry|canvas|three|package(?:-lock)?\.json/i.test(file)),'LFEA-006 changed a prohibited authority path.');
assert.ok(!changed.some((file)=>/^\.github\/workflows\/(?!lfea-006-certification\.yml$)/.test(file)),'LFEA-006 changed a predecessor or aggregate workflow.');
assert.deepEqual(gitLines(['diff','--name-only',BASE_SHA,'HEAD','--','scripts/lfea-001-*','scripts/lfea-002-*','scripts/lfea-003-*','scripts/lfea-004-*','scripts/lfea-005-*']),[],'Predecessor LFEA suites or guards changed.');
const changedCore=changed.filter((file)=>file.startsWith('src/core/element-fea/'));assert.ok(changedCore.every((file)=>file==='src/core/element-fea/index.js'||/\/review-[^/]+\.js$/.test(file)),`LFEA-006 changed an unauthorized existing core module: ${changedCore.join(', ')}`);
const newCore=walk(core).filter((file)=>/review-.*\.js$/.test(file));for(const file of newCore){const text=readFileSync(file,'utf8');assert.ok(!/export\s+default/.test(text),`${basename(file)} uses a default export.`);for(const specifier of [...text.matchAll(/from\s+['"]([^'"]+)['"]/g)].map((row)=>row[1]))assert.ok(specifier.startsWith('.')||specifier.startsWith('node:'),`${basename(file)} imports external dependency ${specifier}.`);assert.ok(text.split(/\r?\n/).length<=300,`${basename(file)} exceeds 300 lines.`);}
validateWorkflow();validateAggregateQa();if(errors.length)throw new Error(errors.join('\n'));console.log(`LFEA-006 exact-baseline source boundary passed for ${changed.length} changed files.`);
function validateWorkflow(){const file=join(root,'.github/workflows/lfea-006-certification.yml');const text=readFileSync(file,'utf8');for(const token of ['Checkout exact authorized implementation head','node scripts/lfea-001-contract-check.mjs','node scripts/lfea-002-contract-check.mjs','node scripts/lfea-003-contract-check.mjs','node scripts/lfea-004-check.mjs','node scripts/lfea-005-check.mjs','node scripts/lfea-006-check.mjs','node scripts/lfea-006-source-guard.mjs'])assert.ok(text.includes(token),`LFEA-006 workflow is missing ${token}.`);for(const token of ['lfea-001-source-guard','lfea-002-source-guard','lfea-003-source-guard','lfea-004-source-guard','lfea-005-source-guard','w10','LAFEA','application-shell','playwright'])assert.equal(text.includes(token),false,`LFEA-006 workflow invokes prohibited authority: ${token}`);}
function validateAggregateQa(){const text=readFileSync(join(root,'scripts/qa-check.mjs'),'utf8');for(const token of ['w10.3-support-attachment-contract-check.mjs','w10.3-restraint-capability-contract-check.mjs','w10.3-support-restraint-property-check.mjs','w10.5-support-load-screening-contract-check.mjs','w10.5-support-load-screening-property-check.mjs','lafea.3-contract-check.mjs','lafea.3-element-check.mjs','lafea.3-solver-check.mjs','lafea.3-patch-check.mjs','lafea.3-stress-energy-check.mjs','lafea.3-determinism-check.mjs'])assert.ok(text.includes(token),`Aggregate QA is missing behavioral check ${token}.`);for(const token of ['npm run check:w10.3:static','w10.3-support-restraint-source-guard.mjs','npm run check:w10.5:static','w10.5-support-load-screening-source-guard.mjs','npm run check:lafea.3','lafea.3-source-guard.mjs'])assert.equal(text.includes(token),false,`Aggregate QA still invokes obsolete path gate ${token}.`);}
function ensureBase(){try{execFileSync('git',['cat-file','-e',`${BASE_SHA}^{commit}`],{cwd:root,stdio:'ignore'});}catch{execFileSync('git',['fetch','--no-tags','--depth=1','origin',BASE_SHA],{cwd:root,stdio:'ignore'});}const resolved=execFileSync('git',['rev-parse',BASE_SHA],{cwd:root,encoding:'utf8'}).trim();assert.equal(resolved,BASE_SHA,'Required LFEA-005 baseline is unavailable.');execFileSync('git',['merge-base','--is-ancestor',BASE_SHA,'HEAD'],{cwd:root});}
function gitLines(args){const text=execFileSync('git',args,{cwd:root,encoding:'utf8'}).trim();return text?text.split(/\r?\n/).sort():[];}
function walk(directory){return readdirSync(directory).flatMap((name)=>{const path=join(directory,name);return statSync(path).isDirectory()?walk(path):[path];});}
